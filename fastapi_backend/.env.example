# PostgreSQL connection env vars (provided by platform/orchestrator)
POSTGRES_URL=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=
# Optional
POSTGRES_HOST=localhost
DB_POOL_MIN=1
DB_POOL_MAX=10

# JWT settings (REQUIRED)
JWT_SECRET=
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=10080

# CORS (optional, comma-separated)
CORS_ALLOW_ORIGINS=http://localhost:3000
